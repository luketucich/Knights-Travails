const possibleMoves = [
  // Up
  [-2, 1], // Down (side) left
  [-1, 2], // Up left
  [1, 2], // Up right
  [2, 1], // Up (side) right

  // Down
  [-2, -1], // Down (side) left
  [-1, -2], // Down left
  [1, -2], // Down right
  [2, -1], // Down (side) right
];
